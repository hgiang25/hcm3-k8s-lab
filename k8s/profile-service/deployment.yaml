apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-service
  namespace: ecommerce
  labels:
    app: profile-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: profile-service
  template:
    metadata:
      labels:
        app: profile-service
    spec:
      containers:
        - name: profile-service
          image: ${CONTAINER_REGISTRY}/profile-service:latest
          ports:
            - containerPort: 3006
          envFrom:
            - configMapRef:
                name: ecommerce-config
            - secretRef:
                name: ecommerce-secrets
          env:
            - name: SERVICE_NAME
              value: "profile-service"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 3006
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: ecommerce
spec:
  selector:
    app: profile-service
  ports:
    - port: 3006
      targetPort: 3006
  type: ClusterIP
